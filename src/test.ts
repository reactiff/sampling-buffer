import SamplingBuffer from './SamplingBufer';
import ClosedCircuitBuffer from './ClosedCircuitBuffer';

describe('SamplingBuffer', () => {
  it('is truthy', () => {
    expect(SamplingBuffer).toBeTruthy()
  })
})

describe('ClosedCircuitBuffer', () => {
  it('is truthy', () => {
    expect(ClosedCircuitBuffer).toBeTruthy()
  })
})
